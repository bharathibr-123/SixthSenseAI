# SixthSense AI — Backend Requirements
# Tata Technologies InnoVent Hackathon 2026
#
# Every version below was actually installed and tested together in the
# sandbox this backend was built in — the full test_app_no_camera.py
# suite (16 steps: DB, all API routes, risk engine, EAR/MAR math,
# calibration, UX-threshold conversion) passed against exactly this set.
#
# Install with:
#   pip install -r requirements.txt
#
# ⚠️ mediapipe note: pip's newer mediapipe builds (0.10.30+) DROPPED the
# classic `mp.solutions.face_mesh` API this code uses, keeping only the
# newer Tasks API. 0.10.13 is the earliest build available for modern
# Python that still has `mp.solutions` — confirmed working here. Do not
# casually upgrade this one without checking `hasattr(mediapipe, "solutions")`
# first. Your original spec pinned 0.10.9, which also has it — use that
# instead if your Python version supports it.

# --- Web framework ---
Flask==3.1.3
Flask-Cors==6.0.5
waitress==3.0.2

# --- Computer vision / ML ---
opencv-python==4.13.0.92
mediapipe==0.10.13
numpy==2.4.4
scikit-learn==1.8.0
joblib==1.5.3

# --- Data ---
pandas==3.0.2

# --- Voice alerts ---
gTTS==2.5.4
pygame==2.6.1

# --- HTTP / location ---
requests==2.33.1
geopy==2.4.1

# --- Emergency WhatsApp alerts (optional — app.py no-ops gracefully if
#     TWILIO_ACCOUNT_SID / TWILIO_AUTH_TOKEN env vars aren't set) ---
twilio==9.10.9
